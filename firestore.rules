rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /weddings/{weddingId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null && request.resource.data.createdBy == request.auth.uid;
      // Only the couple (wedding creator) can delete the entire wedding
      allow delete: if request.auth != null && resource.data.createdBy == request.auth.uid;

      match /photos/{photoId} {
        allow read: if request.auth != null;
        allow create: if request.auth != null;
        // Photo owner OR wedding creator (couple) can delete
        allow delete: if request.auth != null && (
          resource.data.uploaderId == request.auth.uid ||
          get(/databases/$(database)/documents/weddings/$(weddingId)).data.createdBy == request.auth.uid
        );
      }

      match /videos/{videoId} {
        allow read: if request.auth != null;
        allow create: if request.auth != null;
        // Video owner OR wedding creator (couple) can delete
        allow delete: if request.auth != null && (
          resource.data.uploaderId == request.auth.uid ||
          get(/databases/$(database)/documents/weddings/$(weddingId)).data.createdBy == request.auth.uid
        );
      }

      match /albums/{albumId} {
        allow read: if request.auth != null;
        // Only wedding creator can create/update/delete albums
        allow create, update: if request.auth != null &&
          get(/databases/$(database)/documents/weddings/$(weddingId)).data.createdBy == request.auth.uid;
        allow delete: if request.auth != null &&
          get(/databases/$(database)/documents/weddings/$(weddingId)).data.createdBy == request.auth.uid;
      }

      match /guestbook/{entryId} {
        allow read: if request.auth != null;
        allow create: if request.auth != null;
        allow delete: if request.auth != null && (
          resource.data.uploaderId == request.auth.uid ||
          get(/databases/$(database)/documents/weddings/$(weddingId)).data.createdBy == request.auth.uid
        );
      }

      match /rsvp/{entryId} {
        allow read: if request.auth != null &&
          get(/databases/$(database)/documents/weddings/$(weddingId)).data.createdBy == request.auth.uid;
        allow create: if request.auth != null;
      }
    }
  }
}
